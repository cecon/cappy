import { DiscoveredEntity } from "./DiscoveredEntity";
import { DiscoveredRelationship } from "./DiscoveredRelationship";

export class EntityReconciliationService {
  reconcile(entities: DiscoveredEntity[]): DiscoveredEntity[] {
    // ...existing code...
    return entities;
  }
}
