/**
 * @fileoverview Centralized default values for LLM/provider configuration.
 * @module shared/constants/llm-config
 */

/**
 * Default provider base URL.
 */
export const DEFAULT_PROVIDER_BASE_URL = 'https://api.openai.com/v1';

/**
 * Default provider model.
 */
export const DEFAULT_PROVIDER_MODEL = 'gpt-4o-mini';

/**
 * Default provider backend.
 */
export const DEFAULT_PROVIDER_BACKEND = 'openai' as const;

