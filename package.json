{
  "name": "cappy",
  "displayName": "Cappy",
  "description": "AI planning assistant for VS Code — creates structured task plans through conversation. Future: WhatsApp bridge for remote dev workflows.",
  "version": "3.1.9",
  "publisher": "eduardocecon",
  "icon": "src/assets/icon.png",
  "repository": {
    "type": "git",
    "url": "https://github.com/cecon/cappy"
  },
  "engines": {
    "vscode": "^1.105.0",
    "cursor": ">=0.1.0",
    "antigravity": ">=0.1.0"
  },
  "categories": [
    "Other",
    "Snippets",
    "Formatters",
    "Machine Learning"
  ],
  "keywords": [
    "ai",
    "copilot",
    "cursor",
    "chat",
    "planning",
    "tasks",
    "whatsapp",
    "assistant"
  ],
  "main": "./out/extension.js",
  "activationEvents": [
    "onStartupFinished"
  ],
  "capabilities": {
    "languageModel": {
      "enabled": true
    }
  },
  "contributes": {
    "commands": [
      {
        "command": "cappy.whatsapp.connect",
        "title": "Cappy: Connect WhatsApp"
      },
      {
        "command": "cappy.whatsapp.status",
        "title": "Cappy: WhatsApp Status"
      },
      {
        "command": "cappy.openDashboard",
        "title": "Cappy: Open Dashboard"
      }
    ],
    "viewsContainers": {
      "activitybar": [
        {
          "id": "cappy-sidebar",
          "title": "Cappy",
          "icon": "src/assets/sidebar-icon.svg"
        }
      ]
    },
    "views": {
      "cappy-sidebar": [
        {
          "type": "webview",
          "id": "cappy.dashboard",
          "name": "Dashboard"
        }
      ]
    },
    "keybindings": [],
    "configuration": {
      "title": "Cappy",
      "properties": {
        "cappy.llm.preferredModel": {
          "type": "string",
          "enum": [
            "auto",
            "claude-sonnet",
            "gpt-4o",
            "gpt-4"
          ],
          "default": "auto",
          "description": "Preferred language model for Cappy agents (auto = best available)",
          "enumDescriptions": [
            "Automatically select the best available model",
            "Claude Sonnet 4.5 (if available)",
            "GPT-4o",
            "GPT-4"
          ]
        },
        "cappy.bridge.mode": {
          "type": "string",
          "enum": [
            "agent",
            "terminal",
            "auto"
          ],
          "default": "auto",
          "description": "How WhatsApp messages are processed. 'agent' = built-in planning agent, 'terminal' = relay to VS Code terminal (works with Antigravity/Cursor/any AI), 'auto' = terminal if available, else agent.",
          "enumDescriptions": [
            "Use Cappy's built-in planning agent",
            "Relay messages to a VS Code terminal (works with any AI assistant)",
            "Auto-detect: use terminal relay if available, otherwise use built-in agent"
          ]
        },
        "cappy.bridge.chatFilter": {
          "type": "string",
          "enum": [
            "self",
            "group",
            "allow_all"
          ],
          "default": "self",
          "description": "Which WhatsApp messages Cappy should process. 'self' = only messages you send to yourself, 'group' = only from a specific group, 'allow_all' = any message (not recommended).",
          "enumDescriptions": [
            "Only messages you send to yourself (safest — use WhatsApp's 'Message Yourself')",
            "Only messages from a specific group (set group name in cappy.bridge.allowedGroupName)",
            "Process all incoming messages (not recommended)"
          ]
        },
        "cappy.bridge.allowedGroupName": {
          "type": "string",
          "default": "Cappy Dev",
          "description": "Name of the WhatsApp group to listen to (only used when chatFilter = 'group')"
        }
      }
    },
    "chatParticipants": [
      {
        "id": "cappy.chat",
        "name": "cappy",
        "description": "Cappy planning assistant: asks clarifying questions and creates structured task plans (does not execute code directly)",
        "isSticky": true,
        "commands": [
          {
            "name": "help",
            "description": "Get help on how to use @cappy"
          }
        ]
      }
    ],
    "languageModelTools": [
      {
        "name": "cappy_create_file",
        "displayName": "Create File",
        "modelDescription": "Creates a new file in the workspace with the specified content. Use this when the user asks to create a file.",
        "userDescription": "Create a new file in the workspace",
        "toolReferenceName": "createFile",
        "tags": [
          "file",
          "create",
          "workspace",
          "cappy"
        ],
        "icon": "$(new-file)",
        "canBeReferencedInPrompt": true,
        "inputSchema": {
          "type": "object",
          "properties": {
            "path": {
              "type": "string",
              "description": "Relative path from workspace root where the file should be created"
            },
            "content": {
              "type": "string",
              "description": "Content to write to the file"
            }
          },
          "required": [
            "path",
            "content"
          ]
        }
      },
      {
        "name": "cappy_fetch_web",
        "displayName": "Fetch Web",
        "modelDescription": "Fetches content from a web URL (HTML, JSON, or text). Provide the url and optionally a CSS selector.",
        "userDescription": "Fetch content from a web URL",
        "toolReferenceName": "fetchWeb",
        "tags": [
          "web",
          "fetch",
          "http",
          "url",
          "cappy"
        ],
        "icon": "$(globe)",
        "canBeReferencedInPrompt": true,
        "inputSchema": {
          "type": "object",
          "properties": {
            "url": {
              "type": "string",
              "description": "HTTP/HTTPS URL to fetch"
            },
            "selector": {
              "type": "string",
              "description": "Optional CSS selector to extract specific content from HTML"
            }
          },
          "required": [
            "url"
          ]
        }
      },
      {
        "name": "cappy_grep_search",
        "displayName": "Grep Search",
        "modelDescription": "Search for text content in workspace files using regex or plain text. Use this to find specific code patterns, function names, or text strings.",
        "userDescription": "Search file contents",
        "tags": [
          "search",
          "grep",
          "content"
        ],
        "icon": "$(search)",
        "inputSchema": {
          "type": "object",
          "properties": {
            "query": {
              "type": "string",
              "description": "Text or regex pattern to search for"
            },
            "isRegex": {
              "type": "boolean",
              "description": "Whether the query is a regex pattern (default: false)"
            },
            "includePattern": {
              "type": "string",
              "description": "Glob pattern for files to include (e.g., '*.ts')"
            },
            "maxResults": {
              "type": "number",
              "description": "Maximum number of results (default: 100)"
            }
          },
          "required": [
            "query"
          ]
        }
      },
      {
        "name": "cappy_read_file",
        "displayName": "Read File",
        "modelDescription": "Read the contents of a file in the workspace. Use this after finding files to understand their implementation.",
        "userDescription": "Read file contents",
        "tags": [
          "read",
          "file",
          "content"
        ],
        "icon": "$(file-code)",
        "inputSchema": {
          "type": "object",
          "properties": {
            "filePath": {
              "type": "string",
              "description": "Absolute or relative path to the file"
            },
            "startLine": {
              "type": "number",
              "description": "Starting line number (1-indexed, optional)"
            },
            "endLine": {
              "type": "number",
              "description": "Ending line number (1-indexed, optional)"
            }
          },
          "required": [
            "filePath"
          ]
        }
      },
      {
        "name": "cappy_create_task_file",
        "displayName": "Create Task File",
        "modelDescription": "Creates a new task file in .cappy/tasks/ with structured XML format. Use this when the user wants to create a task, feature, bugfix, or any type of development work item. The task file includes a checklist, acceptance criteria, and implementation steps.",
        "userDescription": "Create a new task file for tracking development work",
        "toolReferenceName": "createTask",
        "tags": [
          "task",
          "create",
          "planning",
          "cappy",
          "workflow"
        ],
        "icon": "$(checklist)",
        "canBeReferencedInPrompt": true,
        "inputSchema": {
          "type": "object",
          "properties": {
            "title": {
              "type": "string",
              "description": "Title of the task (e.g., 'Implement JWT authentication', 'Fix login bug')"
            },
            "category": {
              "type": "string",
              "enum": [
                "feature",
                "bugfix",
                "refactor",
                "docs",
                "test",
                "chore"
              ],
              "description": "Type of task: feature (new functionality), bugfix (fix existing issue), refactor (code improvement), docs (documentation), test (testing), chore (maintenance)"
            },
            "description": {
              "type": "string",
              "description": "Optional detailed description of what needs to be done"
            }
          },
          "required": [
            "title",
            "category"
          ]
        }
      },
      {
        "name": "cappy_run_terminal_command",
        "displayName": "Run Terminal Command",
        "modelDescription": "Executes a shell command in the workspace root. Use this to run commands like 'find', 'grep', 'ls', 'cat' to explore the workspace or search for files. Returns stdout and stderr.",
        "userDescription": "Run a terminal command",
        "toolReferenceName": "runCommand",
        "tags": [
          "terminal",
          "shell",
          "command",
          "execute"
        ],
        "icon": "$(terminal)",
        "canBeReferencedInPrompt": true,
        "inputSchema": {
          "type": "object",
          "properties": {
            "command": {
              "type": "string",
              "description": "The shell command to execute (e.g., 'find src -name \"*.ts\"', 'grep -r \"TODO\" .')"
            }
          },
          "required": [
            "command"
          ]
        }
      },
      {
        "name": "cappy_check_task_file",
        "displayName": "Check Task File",
        "modelDescription": "Checks whether a task file exists under .cappy/tasks. Useful to verify if a task was already created.",
        "userDescription": "Check if a task file exists",
        "toolReferenceName": "checkTaskFile",
        "tags": [
          "task",
          "check",
          "file",
          "exists"
        ],
        "icon": "$(file-symlink-file)",
        "canBeReferencedInPrompt": true,
        "inputSchema": {
          "type": "object",
          "properties": {
            "fileName": {
              "type": "string",
              "description": "File name inside .cappy/tasks (e.g., task_2025-12-21_123.ACTIVE.xml). If no extension is provided, .ACTIVE.xml and .xml will be tried."
            }
          },
          "required": [
            "fileName"
          ]
        }
      },
      {
        "name": "cappy_create_todo",
        "displayName": "Create Todo",
        "modelDescription": "Creates a new todo item. Use this when the user wants to create a task, add something to their todo list, or remember something to do.",
        "userDescription": "Create a new todo item",
        "toolReferenceName": "createTodo",
        "tags": [
          "todo",
          "task",
          "create",
          "cappy"
        ],
        "icon": "$(add)",
        "canBeReferencedInPrompt": true,
        "inputSchema": {
          "type": "object",
          "properties": {
            "title": {
              "type": "string",
              "description": "Title of the todo (e.g., 'Buy groceries', 'Review PR #123')"
            },
            "description": {
              "type": "string",
              "description": "Optional detailed description of the todo"
            }
          },
          "required": [
            "title"
          ]
        }
      },
      {
        "name": "cappy_list_todos",
        "displayName": "List Todos",
        "modelDescription": "Lists all todo items with their status. Use this when the user asks to see their todos, check what tasks they have, or review their todo list.",
        "userDescription": "List all todo items",
        "toolReferenceName": "listTodos",
        "tags": [
          "todo",
          "list",
          "view",
          "cappy"
        ],
        "icon": "$(checklist)",
        "canBeReferencedInPrompt": true,
        "inputSchema": {
          "type": "object",
          "properties": {}
        }
      },
      {
        "name": "cappy_complete_todo",
        "displayName": "Complete Todo",
        "modelDescription": "Marks a todo as completed. Use this when the user indicates they finished a task or want to mark something as done.",
        "userDescription": "Mark a todo as completed",
        "toolReferenceName": "completeTodo",
        "tags": [
          "todo",
          "complete",
          "done",
          "cappy"
        ],
        "icon": "$(check)",
        "canBeReferencedInPrompt": true,
        "inputSchema": {
          "type": "object",
          "properties": {
            "id": {
              "type": "string",
              "description": "ID of the todo to mark as complete"
            }
          },
          "required": [
            "id"
          ]
        }
      }
    ]
  },
  "scripts": {
    "lint": "eslint .",
    "test": "vitest",
    "test:ui": "vitest --ui",
    "test:run": "vitest run",
    "test:coverage": "vitest run --coverage",
    "vscode:prepublish": "npm run compile-extension",
    "compile-extension": "tsc --project tsconfig.extension.json",
    "watch": "tsc -watch -p ./",
    "package": "npm version patch && npm run compile-extension && vsce package --dependencies",
    "install:extension": "node scripts/install-extension.js",
    "publish": "npm run package && vsce publish && code --install-extension cappy-*.vsix"
  },
  "dependencies": {
    "@langchain/core": "^0.3.78",
    "@langchain/langgraph": "^0.4.9",
    "@whiskeysockets/baileys": "^7.0.0-rc.9",
    "ws": "^8.19.0"
  },
  "devDependencies": {
    "@eslint/js": "^9.36.0",
    "@types/node": "^24.9.1",
    "@types/vscode": "^1.105.0",
    "@types/ws": "^8.18.1",
    "@vitest/ui": "^3.2.4",
    "@vscode/vsce": "^3.6.2",
    "eslint": "^9.36.0",
    "globals": "^16.4.0",
    "rimraf": "^6.0.1",
    "typescript": "~5.9.3",
    "typescript-eslint": "^8.45.0",
    "vitest": "^3.2.4"
  }
}
