# Exclude large/unnecessary folders from the VSIX
# Note: We need node_modules for production dependencies like @lancedb/lancedb
# The --dependencies flag should handle including only production deps
old/
docs/
.vscode/
.git/
.gitignore
.vscodeignore
**/*.map
**/*.test.*
**/__tests__/**
# Exclude sharp - not used for text embeddings, only for image processing
node_modules/**/sharp/**
node_modules/**/@img/sharp-*/**
# Keep built output and extension entry
!out/**
!src/assets/**
# Include package.json, README, LICENSE by default
